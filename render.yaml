services:
  - type: web
    name: cotizador-peltre
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: ENVIA_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
