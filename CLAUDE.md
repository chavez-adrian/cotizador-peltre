# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev          # desarrollo con hot-reload (--watch)
npm start            # produccion
npm test             # todos los tests (0 fallas esperadas)

# Correr un test individual:
node --test test/server.test.js
node --test --test-concurrency=1 public/js/__tests__/alta-csf.test.cjs
```

> `--test-concurrency=1` es obligatorio cuando los tests comparten estado global (`globalThis.fetch` mock o `cotizaciones.json`). Sin el los tests se interfieren.

## Documentos de contexto — leer ANTES de tocar el area

- `docs/arquitectura.md` — **detalle por modulo/tema** (lib/, persistencia, catalogos, auth, quirks de Operam). Este CLAUDE.md es el resumen; el detalle vive alla.
- `CONTEXT.md` — glosario de dominio (el glosario manda) + `docs/adr/` — decisiones de arquitectura (0001-0017).
- `PROCESO_COMERCIAL_AS_IS.md`, `SOP_crear_cliente_operam.md`, `MAPEO_CAMPOS_CLIENTE.md` — proceso comercial y flujo de clientes; leer antes de cambios a ese flujo.
- `peltre-operam.md` (raiz `_Claude/`) §12 — API de Operam: tipos de transaccion REALES (el MCP `operam-api` los etiqueta mal), cadena `order_`, contrato de escritura del quote. Consultar ANTES de explorar Operam.

## Arquitectura

Servidor Express monolitico (`server.js`) con frontend vanilla JS (`public/js/app.js`). Sin frameworks frontend, sin bundlers.

```
Browser (app.js) → /api/*                        → server.js → lib/* → Operam v3 API / envia.com
                                                             → data/*.json (persistencia en disco)
                                                             → pdfkit / html-generator
                 → /api/crear-cliente             → lib/operam-client.js → Operam
                 → /api/buscar-cliente            → lib/db.js            → Neon (clientes_log)
                 → /api/actualizar-cliente/:id                           → lib/dropbox.js → Dropbox
                 → /api/csf-from-url              → SAT (proxy QR)
```

Patron de la casa: **nucleos PUROS sin IO** compartidos por cross-import entre `server.js` y `public/js` (un modulo, varios consumidores, cero copias espejo). Ejemplos: `alta-logica.js`, `calcas-logica.js`, `cruce-identidad.js`.

### Modulos lib/ (una linea; detalle en docs/arquitectura.md)

| Modulo | Que hace |
|--------|----------|
| `operam-client.js` | Bearer auth con auto-refresh; `buildClienteBody` = UNICO mapeo cliente→Operam (ahi viven los overrides fiscales del RFC generico #121); `buscarClientes` (por NOMBRE) vs `buscarClientesPorRfc` (pool por RFC, #194) no son intercambiables; lectores read-only con retry y throttle anti-429 |
| `lista-precios-cliente.js` | Nucleo puro del cliente sin lista de precios (#285): `clienteSinListaPrecios` (sales_type 0/''/null), el texto accionable y `esErrorRateMoneda` (traduce el 406 "Debe haber al menos un rate de moneda", que en realidad dice que al CLIENTE le falta lista) |
| `cel-operam.js` | Nucleo puro de la casilla **Cel** de Operam (#339, ADR-0016): `CAMPO_CEL` = `fax` (asi viaja en la API lo que la web etiqueta "Cel No.") y `celsNoAplicados`, la verificacion por relectura que comparten los dos caminos del alta; compara por `ultimos10`, no por texto, y el unico lector es `GET /customers/:id` (el `GET /branches/:code` NO expone la llave) |
| `alta-cliente.js` | EL modulo **Alta de cliente** (#364, ADR-0017): `darDeAlta(solicitud, deps = {})` recorre la secuencia unica del alta -- dedup en capas, crear o reutilizar, dimensiones, domicilio de entrega (POST solo si no hay uno equivalente; PUT **solo sobre el recien creado**, con una bandera que decide el modulo), relectura y comparacion SIEMPRE, Cel -- y devuelve VALORES: alta lograda, pregunta al vendedor (candidatos con sus salidas, o el celular ligado a otra razon social) o bloqueo con motivo. Nunca un status HTTP, y no importa cotizaciones ni Express: los handlers solo traducen. Cada paso lleva `mensaje` del glosario y `detalle` tecnico (**Mensaje en dos capas**), y el que no aplica sale `omitido` con su motivo. Operam, el padron, la auditoria (`clientes-log.js`), el vendedor y la liga del Contacto entran como `deps` con fallback a las funciones reales; el adaptador en memoria de los tests implementa las mismas (`test/helpers/operam-memoria.js`, cero `fetch`). Devuelve el `clienteId` tambien en el bloqueo: quien pide el alta lo persiste antes de responder y por eso un reintento no crea un segundo cliente. #365: el ULTIMO paso es el **segmento** -- con `esperar` el alta espera la escritura por la web legacy y la reporta como un paso mas, y con `diferido` (la subida de la cotizacion) la devuelve como `segmentoDiferido()` para que la subida la dispare despues de responder y anota el fallo en `clientes_log` como `segmento-pendiente`, que `segmento-pendiente.js` convierte en la lista del panel `/admin` #367: la otra operacion del modulo es `upgradeFiscal(id, datosFiscales, deps = {})` -- gate anti-fusion por RFC exacto, PUT con el mapeo de `DIFF_FISCAL_CAMPOS` y su eco, post-fix del segmento por la web SIN `soloSinSegmento` (la via para corregir un segmento mal asignado, #253) y relectura que devuelve los campos no aplicados en dos capas -- solo los que Operam recibio y no guardo: los que el modulo decidio no mandar viajan en `camposFueraDeLaVerificacion` y salen de esa lista (#379), porque ya se reportaron como paso propio con su motivo real; nunca crea un Cliente Operam y no pisa el nombre corto que el cliente ya tenia (#360) #366: con `datosFiscales` el cliente nace con su RFC real, la dedup parte del RFC exacto, el uso de CFDI viaja en el PUT (el POST lo ignora, #217), la configuracion comercial de un cliente reutilizado solo escribe lo que cambia, y el lock por RFC (`conLockPorRfc`) y el refresco del padron pasaron de `server.js` a aqui #377: las **salidas dependen del motivo del candidato** -- el que coincide por RFC real EXACTO (marcado por COMPARACION de `tax_id` al final del pool, no por el camino que lo trajo) es el mismo contribuyente, asi que la pregunta no ofrece `ninguno` (y el mensaje dice por que), la decision `ninguno` que llegue igual sale como bloqueo `fusion` con la redaccion de #375 y sin `clienteId`, y en el navegador la salida que no viene no se pinta (sin informacion de salidas, las tres) #368: `decision` de tipo `usar` acepta ademas el `domicilioId` que el vendedor eligio entre los del Cliente Operam (y lo revalida como al candidato: uno ajeno vuelve a preguntar), asi la cotizacion deja de heredar el primero de la lista (#252), y la `pregunta` de candidatos es lo que el handler traduce al 428 `POSIBLE_DUPLICADO` con sus salidas ya serializadas como cuerpos de reintento |
| `alta-generica.js` | Constructores PUROS del alta con RFC generico (#81/#83) + PUT del branch con domicilio de entrega (#96). Desde #364 son el **seam interno** de `alta-cliente.js`; la orquestacion vive alla |
| `deduplicacion.js` | RFC genericos + dedup (#78); `DEBTORS_GENERICOS`; `normalizarNombre`; `poolClientesParaDedup` = EL armador del pool que comparten los dos sitios que combinan listas de clientes (#364) |
| `cruce-identidad.js` | Nucleo puro del cruce por identidad (#123): CERRO/COMPRO_OTRA_COSA/SIN_SENAL, banda ±15%, normalizacion de telefono |
| `telefono-llave.js` | `ultimos10` = la UNICA llave de identidad de prospecto |
| `referencia-cliente.js` | Nucleo puro de la Referencia del cliente (#241): cadena `referencia -> nombreCorto -> nombreEntrega -> razonSocial` que comparten el `cust_ref` del quote y el documento (PDF/HTML), truncado a 60 incluido (paridad exacta con Operam); normaliza SOLO el escalon de razon social (llega en MAYUSCULAS del SAT), con el titulador unico de #293 |
| `contactos-observabilidad.js` / `contactos-observabilidad-io.js` / `contactos-observabilidad-store.js` | Observabilidad de los barridos de sync de contactos (#230): estado por nombre de barrido en Neon (ultima corrida, ultima EXITOSA, totales, errores clasificados autorizacion/red/datos/otro) + correo diario de aviso si se supera el umbral sin corrida exitosa, reusando la envoltura SMTP de `alerta-mayoreo-io.js`. Vista en `/admin` junto a higiene de clientes |
| `contactos-logica.js` / `contactos-io.js` / `google-contactos.js` / `contactos-store.js` | Sync de contactos a la libreta de Google para que WhatsApp Business muestre nombres (#224/#227/#228/#229/#231, ADR-0013): nucleo puro que devuelve el plan `{crear, actualizar, inactivar, errores}` con **mascara explicita por entrada** — la del `adoptado` (contacto que ya estaba en la libreta) es CORTA a proposito y **unificarla con la del propio borra telefonos y correos ajenos cada 15 min** — desde DOS fuentes — prospectos y telefonos de clientes de Operam, que se anotan despues y por eso GANAN (precedencia de ADR-0013) + motor que lo ejecuta SECUENCIAL + cliente REST de People API (calcado de `dropbox.js`, cero dependencias) + mapeo `celular10 -> resourceName/etag/clase/huella` en Neon. `aFormatoWhatsApp` es EL punto unico del formato del telefono (#226). Lo que pierde respaldo NO se borra: pasa al grupo `Cotizador inactivos` y deja de actualizarse (#231), con **tope de seguridad** (>20% de las fichas propias activas Y >5 en una pasada) y freno "sin evidencia" si la fuente de clientes llega vacia — los dos salen como error de datos al panel, nunca como inactivaciones |
| `pedidos-shopify-logica.js` / `pedidos-shopify-io.js` / `pedidos-shopify-store.js` / `shopify-pedidos.js` | Clientes en linea: los pedidos de la tienda como TERCERA fuente de la libreta (#255/#256, ADR-0014). Nucleo puro `nodo de pedido -> { filas, descartes }` con la regla del telefono en tres escalones: 1) codigo de pais explicito o pegado que `libphonenumber` declare VALIDO (la direccion no se consulta); 2) sin codigo, se completa con el pais de envio/facturacion SOLO si no se contradicen y `isValid(numero, pais)` lo confirma; 3) todo lo demas (paises contradictorios, sin pais, invalido) se descarta con motivo. El escalon 1 se resuelve para TODOS los candidatos antes de intentar el escalon 2 en ninguno, para que un candidato sin codigo no se reporte si otro candidato del mismo pedido ya trajo el mismo numero CON codigo, sin importar el orden. + sondeo horario incremental por cursor `updated_at` + tabla `pedidos_shopify` en Neon (fallback JSON) + lector GraphQL (REST es legacy; `first:100` por el tope de costo; `customer.phone` esta deprecado, va `defaultPhoneNumber`). Los pedidos se anotan PRIMERO en `planificarContactos` = ultimo escalon de la precedencia (Operam > prospecto > en linea), y una tabla vacia frena TODA inactivacion igual que la fuente de clientes |
| `higiene-clientes.js` | Reporte admin "Clientes genericos sin actividad" (#86) |
| `segmento-pendiente.js` | Reporte admin "Clientes con segmento pendiente" (#365): nucleo PURO sobre `clientes_log` -- que Clientes Operam siguen sin su segmento escrito, con el motivo en dos capas y la liga a Operam. El segmento intentado viaja en `fuente` (`segmento:<id>`), que ademas mantiene estas filas fuera del reporte de higiene. Una fila desaparece cuando existe una posterior del mismo cliente con resultado `segmento-escrito` |
| `contacto-cotizacion.js` | Nucleo PURO de la liga FIJA Oportunidad -> Contacto (#342, ADR-0016): `celularAlNacer` (lo que se anota al crear la cotizacion, en la columna `contacto_celular`), `celularesDeCruce` (por donde cruzan tablero, Historial, Hoy y Tabla de prospectos; sin campo propio cae a lo tecleado) y `contactoDeCotizacion`/`planearMigracion` con las cuatro fuentes de la migracion, en orden (cruce, telefono anotado, indice de Operam con Cel > Telefono > Secundario, sin Contacto). La guarda de "fija" vive en `cotizaciones-store.setContactoCelular`, que nunca pisa una liga existente |
| `ligas-contacto.js` | Nucleo PURO de la liga Contacto <-> Cliente Operam, que es MUCHOS A MUCHOS (#345, ADR-0016): `data.cliente_id` pasa a la lista `data.clientes_operam` `[{cliente_id, fuente}]` y la singular sobrevive como la PRIMERA liga (los lectores viejos no cambian). Dos fuentes: `cotizador` (la unica que se persiste) y `operam` (`conLigaDerivada`, se calcula del indice de telefonos EN LECTURA y nunca se guarda; es informacion, jamas un bloqueo). `decidirLiga` decide `nada`/`confirmar`/`agregar` al subir |
| `oportunidades.js` | Nucleo PURO de las tarjetas del tablero (#340, ADR-0016): `tarjetasOportunidades(prospectos, cotizaciones)` -> una tarjeta por Oportunidad, sin la del prospecto que ya cotizo (liga por `cotizacionesDelProspecto`); lo sirve `GET /api/oportunidades`, el UNICO endpoint del tablero. `cotizacionesDeLaOportunidad` (#343) afina la supresion: a una Oportunidad con registro propio solo la calla la cotizacion que nacio de ELLA |
| `estado-cliente-operam.js` / `etiquetas-contacto.js` / `actividad-operam.js` | Estados del Cliente Operam y etiquetas del Contacto (#344, ADR-0016), DERIVADOS y jamas guardados. Fiscal: RFC generico (o sin RFC) -> Sin datos fiscales. Comercial: pedido vivo -> con pedido, quote vivo -> cotizado, si no sin actividad; `fuenteIncompleta` declara el hueco de los quotes web NO enumerables por la API, y se marca solo cuando el veredicto es "sin actividad" (es la unica direccion en que el hueco puede fallar). `construirActividad` mezcla los pedidos que la API si enumera (+ su `trans_no_from`) con el espejo local (#67) y `data/cancelados.json`. `esProspecto` = hay ficha y NO `sinCaptura`: es LA definicion de la etiqueta y la consumen `clasificar-celular.js` y la libreta de Google (#342 hizo que "tener fila" dejara de significar "ser prospecto"). Las etiquetas se acumulan: una Oportunidad Perdida no quita ninguna. `actividad-operam.js` es la envoltura con IO (barrida paginada de pedidos, cache TTL 1 h con warm al arrancar) y NO inventa: un `customer_id` que el padron cacheado no conoce viaja sin estados. El vocabulario vive en `public/js/estado-cliente-logica.js` (cross-import lib -> public/js) |
| `buscador-clientes.js` | Nucleo PURO del buscador de la vista Clientes por CONTACTO (#346, ADR-0016): `filasBuscadorClientes` devuelve **una fila por Contacto** con sus Clientes Operam ANIDADOS, mas las filas de los Clientes Operam sin ningun Contacto conocido. Un Cliente Operam con dos Contactos aparece bajo los dos y **ya no como fila suelta** (es la duplicacion de Jorge Orea que cierra el ticket); un Contacto con dos razones sociales sale UNA vez con las dos. Lo sirve `GET /api/contactos/buscar`, que comparte con `GET /api/operam/clientes` (el buscador del paso Cliente, que NO cambia) `clientesOperamPorTexto`, `celularesDeClienteOperam` y `filaClienteOperam` |
| `oportunidad-pre.js` / `oportunidades-store.js` / `oportunidad-pre-io.js` | La Oportunidad pre-cotizacion vive APARTE del Contacto (#343, ADR-0016): la tabla `prospectos` es la de **Contactos** (persona) y la tabla `oportunidades` guarda la intencion (contacto, vendedor, etapa, eventos, toques, reunion, siguiente contacto). Un Contacto, varias Oportunidades. Nucleo puro que fusiona las dos en la forma de siempre + store (Neon con fallback JSON) + escritura que va a donde la Oportunidad vive. **Mientras la separacion no ha corrido, el Contacto SIGUE siendo su propia Oportunidad con su mismo id** (por eso el deploy puede llegar antes que `node scripts/migrar-oportunidades.mjs --apply`), y las dos tablas comparten espacio de ids para que ese id nunca sea ambiguo. `POST /api/oportunidades` = "Nueva oportunidad" (solo celular, sin origen: lo hereda del Contacto) |
| `tabla-prospectos.js` | Nucleo PURO de la Tabla de prospectos (#306, tickets #313-#323): `estadoProspecto` y `filaTabla` traducen la Oportunidad a la fila que pinta `/prospectos` (`/leads` redirige ahi); el navegador NO calcula derivados, los pide a `GET /api/prospectos/tabla`, que desde #343 devuelve una fila por OPORTUNIDAD (no por persona). #400: `faltaCotizar(p, cotizaciones)` es EL predicado de la etiqueta "Ya tiene Cliente Operam, falta cotizar" -- liga Y ninguna cotizacion, nunca la liga a secas (el Contacto que el cotizador dio de alta AL cotizar la llevaba puesta) -- y lo derivan las rutas que pintan la etiqueta (`GET /api/prospectos`, `/api/prospectos/cola` y `/api/hoy`) con `oportunidadesQueFaltaCotizar` y la visibilidad del vendedor; el navegador solo pinta la senal que recibe |
| `sync-operam.js` / `sync-operam-io.js` / `sync-operam-webhook.js` | Sync post-venta (#62): nucleo puro + motor de reconciliacion + webhook; binding SOLO por `data.orderOperam` |
| `backfill-operam.mjs` | Nucleo puro del backfill historico (#76); excluye cancelados |
| `recolector-genericos.mjs` | Lote historico de quotes de debtors genericos → bandeja (#124); orquestado por `scripts/rescatar-genericos.mjs` |
| `catalogo-operam.js` | Catalogo generado desde Operam (#128/#131); orquestado por `scripts/sync-catalogo.mjs`. Desde #298 incluye las **listas sin escalon** (Segundas la primera; #299 las ocho de hoy), que NO son una tabla en codigo: son las sales_types ACTIVAS de Operam que no son escalon de volumen (`listasSinEscalon`, ordenadas por id del ERP; la inactiva no entra aunque tenga filas de precio vivas -- Bazaar tiene 461 -- y la que no trae factor legible tampoco), van al final de `tiers` SIN `min_qty` (jamas tabulan) y se precian con la regla de siempre -- fila explicita gana (las 33 filas de calca de M6001), si no base x factor (Amazon, 1.1, es la unica POR ENCIMA del precio base; las US aplican su factor en PESOS, la moneda la manda el cliente), y sin precio base quedan en `null`. Una lista nueva en el ERP queda cotizable con solo regenerar el catalogo |
| `operam-web.js` | Web legacy (FrontAccounting): vigencia (#106) + actualizar quote conservando folio (#104) + ronda de descripcion por partida (#139) + post-fix del segmento del cliente (#172) + deteccion de cancelados |
| `config-store.js` | Configuracion del panel `/admin` (tipos y texturas activos, Evento activo, ligas del sitio y del catalogo) en Neon (#276); `data/config.json` es SEMILLA de la tabla vacia y fallback sin `DATABASE_URL`; cache en memoria porque los lectores son sincronos (`leer()`), refrescada al guardar y calentada al arrancar (`cargar()`) |
| `modelos-store.js` | Bloque de modelos del **Maestro de articulos** (36 filas x 32 columnas) en Neon (#310, ADR-0016); `data/modelos.json` es SEMILLA de la tabla vacia y fallback sin `DATABASE_URL`; `actualizar(modelo, campos)` solo acepta las columnas de `CAMPOS_EDITABLES`, el resto es de solo lectura hasta #304; `sinFamilia()` son los pendientes que el panel `/admin/catalogo` resalta |
| `vendedores-store.js` | Registro de vendedores (identidad, PIN en claro, rol, operam_id, tope) en Neon (#140/#141); auto-siembra desde `data/vendedores.json` si la tabla esta vacia; el PUT de admin reemplaza el registro completo |
| `db.js` | Pool pg; `query()` retorna null sin pool (graceful); auto-crea `clientes_log` y `operam_webhooks_log` en Neon |
| `clientes-log.js` | Auditoria de altas y cambios de cliente (`clientes_log`), fire-and-forget. Desde #356 `logCliente` devuelve el id de la fila que inserto (`darDeAlta`/`upgradeFiscal` lo sacan como `logId`) y `marcarDropbox(logId, ok)` la corrige cuando la subida de la constancia resuelve: por eso `dropbox_ok` dejo de ser siempre null |
| `dropbox.js` / `dropbox-destinos.js` / `dropbox-subidas-store.js` | OAuth refresh; `upload({flujo, archivo}, content, mode)` es el ENVOLTORIO COMUN de las tres subidas (constancia fiscal, posicion de calca, backup del export de Bitrix) y el unico punto donde pasan las dos mitades del tema. **Destino** (#357): lo decide el FLUJO -- `csf`/`calca`/`bitrix`, declarados UNA vez en `dropbox-destinos.js`, que es tambien la llave del registro --; con `DROPBOX_NS_*` + `DROPBOX_PATH_*` la peticion lleva `Dropbox-API-Path-Root` con el `namespace_id` y ruta RELATIVA, y sin las DOS variables cae a la ruta heredada del sandbox y avisa por consola; con namespace un error RECHAZA y no se reintenta contra la ruta de texto (eso reintroduciria #354). **Registro** (#356): cada intento -- flujo, destino pretendido, archivo, resultado, error y momento -- queda en `dropbox_subidas` (Neon, fallback `data/dropbox-subidas.json`), que el panel `/admin` muestra con los fallos marcados; NUNCA lanza y no altera el contrato fire-and-forget. `subirCsfDropbox` = backup de CSF |
| `parsear-csf.js` | Puro: extrae RFC/razon social/domicilio/regimen del PDF de CSF **y del validador QR** (#378: otras etiquetas -- `CP`, `Municipio o delegacion`, `Entidad Federativa`, `Colonia`, `Apellido Paterno` -- y el valor EN LA LINEA SIGUIENTE a su etiqueta, que solo se pliega si abajo no viene otra etiqueta); el catalogo del SAT lo cross-importa de `public/js/regimen-fiscal-logica.js` (#191) |
| `sat-qr.js` | Validador QR del SAT (#378), el respaldo cuando el PDF no da RFC: `esHostDelSat`, `htmlATexto` y la descarga por `node:https` -- **no `fetch`**, porque el SAT negocia un DH corto que OpenSSL 3 rechaza (`ERR_SSL_DH_KEY_TOO_SMALL`) y solo `opcionesTlsPara` (SECLEVEL=0 acotado a sus hosts) lo salva. `node scripts/verificar-csf-qr.mjs <url del QR>` lo verifica EN VIVO (read-only); ningun mock puede |
| `pdf-generator.js` / `html-generator.js` | PDFKit / HTML auto-contenido para WhatsApp, mismo formato visual: el PDF es la **especificacion de referencia** (geometria medida del formato oficial de Operam) y el HTML la traduce a CSS fluido con **paridad en tres capas** (#326) -- contenido y etiquetas EXACTAS, escritorio e impresion fieles (colores medidos, cero lineas verticales, cajas de solo trazo gris), y en telefono (<=480px) manda la legibilidad con el reflow de tarjetas de #302. La duplicacion de layout entre los dos es deliberada; sus diferencias estan declaradas en el encabezado de `html-generator.js` |
| `calcular-envio.js` | Carrito → paquetes fisicos para envia.com; excluye `ENVIO` y la calca |
| `extract-prices.js` | LEGADO desde el corte #131: contraste contra el Excel, ya no genera `data/precios.json` |
| `validar-cp.js` | Puro: valida CP por pais |
| `fs-reintento.js` | TODO acceso a `data/*.json` pasa por aqui (ver Trampas) |

`public/js/titulo-logica.js` (EL titulador del repo, #293: `aTitulo` es la UNICA regla "texto gritado -> Titulo", con la guarda de intencion que respeta lo ya escrito en mezcla; lo consumen la Referencia del cliente, el Contacto de Google, `normalizarTextosProspecto` y el importador del export de expo), `public/js/alta-logica.js` (logica pura del alta de cliente, incluida la tabla `REGLAS_TELEFONO` que comparten `validarTelefono` y `telefonoValido`), `public/js/calcas-logica.js` (calca en el carrito, #91/ADR-0010; ahi vive tambien el precio manual de calca de #279: `precioEfectivoCalca` para la linea y `validarPreciosManualesCalca` para el POST; el permiso por vendedor es #280: `puedePrecioCalca`/`normalizarPuedePrecioCalca`), `public/js/regimen-fiscal-logica.js` (catalogo c_RegimenFiscal del SAT + filtro por tipo de RFC, #191; lo consumen el selector del alta y `lib/parsear-csf.js`) , `public/js/telefono-widget.js` (widget intl-tel-input vendoreado + capa estricta que avisa sin bloquear, #176; lo comparten mayoreo y los 6 campos de telefono del alta interna), `public/js/origen-logica.js` (nucleo del **Origen** y su herencia, #287: `origenDe`/`anotarOrigen` sobre un indice por celular -- la cotizacion y el cliente NO guardan origen, lo heredan del prospecto de su mismo celular; desde #340 lo resuelven SIEMPRE los GET del servidor -- el tablero (`/api/oportunidades`), el Historial y Hoy; el chip `chipOrigenHtml` vive en `prospectos-logica.js` para no cerrar un ciclo con `escapeHtml`) y `public/js/busqueda-logica.js` (buscador en vivo de las listas, #289: `filtrarPorCriterio` sobre `{texto, desde, hasta}` con AND; cada vista declara sus `BUSCABLES_*` — `camposDe`/`digitosDe` y `fechaDe` (instante, se compara por su dia LOCAL) o `diaDe` (dia calendario ya resuelto, literal: Rescatados); lo consumen Historial, Prospectos, Rescatados, Pipeline y Hoy) y `public/js/cp-autollenado.js` (regla de NO PISAR del autollenado por CP, #291: `decidirCampoAsistido` / `planAutollenadoCP` / `paisTieneIndiceCP`; la comparten el paso Envio y el alta completa, NO la captura de expo ni mayoreo -- ahi el campo Ciudad es respaldo, otra semantica) y `public/js/resumen-cotizacion-logica.js` (EL constructor del **Resumen de la cotizacion**, #307/#311/#312: `mensajeCotizacion(registro, origin, indiceFamilias)` -> `{texto, waUrl}` o `null` sin id o sin folio (`motivoSinResumen`); registro plano `{id, folioOperam, cliente, nombreCorto, vigencia, total, items}`, un renglon por Familia de producto ordenado por importe (`Familia · N modelos · N pzs`, sin importes por renglon), calca aparte en piezas decoradas, envio SIEMPRE, dinero una sola vez al cierre; el indice `modelo -> familia` viaja en `/api/precios` como `familias`; liga SIEMPRE al HTML, nunca al PDF; lo comparten `shareWhatsApp` de `app.js` y `buildWhatsAppLinkHistorial` del historial, que quedo como envoltura) y `public/js/moneda-cliente-logica.js` (EL juicio del bloqueo por **Moneda del cliente**, #297/ADR-0015: `bloqueoMonedaCliente(cliente, nombre)` -> `null` o `{moneda, mensaje}`; el cotizador solo sabe pesos y Operam los etiquetaria con la moneda del cliente (17x), asi que un cliente con `curr_code` distinto de MXN se detiene en los dos puntos -- el paso Cliente (la fila trae `moneda`; sin senal no bloquea) y la subida del quote, que responde 422 `CLIENTE_MONEDA_EXTRANJERA` sin POST; lo levanta #295) y `public/js/estado-cliente-logica.js` (EL vocabulario de los dos estados del Cliente Operam y, desde #346, tambien de las **etiquetas del Contacto** (`ETIQUETAS_CONTACTO_ORDEN` / `ETIQUETA_CONTACTO`, que `lib/etiquetas-contacto.js` reexporta): las cadenas y su texto de pantalla — "Sin datos fiscales" / "Con datos fiscales", "con pedido" — en un solo lugar, del que `lib/estado-cliente-operam.js` los cross-importa para que el servidor que decide y el navegador que pinta no diverjan) y `public/js/tier-logica.js` (Auto vs **Lista fijada** y el permiso de lista, #151/#153/#154/#296: desde #296 el permiso es una MATRIZ (vendedor, lista de Operam) -- `listasHabilitadasDeVendedor` normaliza el renglon del registro y MIGRA de lectura el flag binario de #153 (encendido y sin campo nuevo = los escalones de volumen), y `puedeFijarTier` cruza tier y celda por `listaId`, el id de `sales_type` que el catalogo expone ahora en cada tier; una lista marcada que el catalogo no precia no vuelve fijable nada; #298: `esEscalonDeVolumen` -- los tiers CON `min_qty` -- es lo UNICO que tabula, y lo comparte `server.js` para que la migracion de lectura del flag de #153 reparta escalones de volumen y nunca una lista sin escalon como Segundas; #300: la MISMA matriz gobierna la SEGUNDA superficie -- la lista que se le ASIGNA a un cliente en el alta y en la edicion (`opcionesListaCliente` para el selector, `validarListaCliente` para el guardado) --, donde el universo son las sales_types ACTIVAS de Operam y no los tiers del catalogo (a un cliente se le puede asignar una lista que el cotizador aun no sabe preciar), y conservar la que el Cliente Operam YA tiene siempre es valido) son los nucleos puros del frontend — detalle y reglas no obvias en `docs/arquitectura.md`.

## Trampas que cuestan horas (no derivables del codigo)

- **`onclick` inline resuelve contra `window`, no contra el modulo (#112).** Si existen `function foo()` de modulo Y `window.foo = ...`, el menu dispara el de `window` y el otro queda muerto — sin error, sin sintoma en tests. Regla: un simbolo por nombre; lo invocado desde `onclick` se expone a `window` JUNTO a su declaracion. Esto NO lo ve un code review: solo aparece **ejecutando en navegador**.
- **El panel del alta y el del upgrade fiscal son EL MISMO nodo (#376)**: `pcAbrirUpgradeFiscal` hereda las Secciones 3 y 4 que un alta anterior de la MISMA pestana dejo desbloqueadas, y desde ahi "Dar de alta" mandaba un `POST /api/crear-cliente` con la CSF del upgrade (un upgrade bloqueado por el gate de fusion degeneraba en un alta del Cliente Operam que ya existia). El candado de las secciones y el boton apagado son lo que el vendedor ve; lo que lo hace cierto es `errorAltaEnModoUpgrade` en `altaEnviarAlta` -- EL punto del `POST`, por donde pasan tanto el boton como las tres salidas del 428 --, que mira el MODO (`altaCsfState.modoUpgrade`) y no el DOM. Quien prenda ese modo tiene que dejar salida: "Crear nuevo" del dedup por RFC generico lo apaga.
- **Llenar los campos `cl-*` NO es reponer la identidad (#394)**: `cargarCotizacion` (Editar y Copiar del historial) los llenaba con la cotizacion cargada y dejaba `pcState.cliente` -- de donde sale el `customerId` que viaja -- en el cliente de la sesion ANTERIOR, asi que la 1280 se guardo con el nombre y el domicilio de Sofia y el Cliente Operam de Gerardo. La identidad se repone con `clienteAlCargarCotizacion` (`cotizaciones-logica.js`) y la liga del registro es FIJA en el servidor (`ligaClienteAlGuardar`: un id distinto del persistido no lo pisa); en Copiar, que nace sin registro previo, el navegador es la UNICA red.
- **`lib/fs-reintento.js` es obligatorio para `data/*.json`** (#117), en stores Y helpers de tests: OneDrive toma locks EBUSY intermitentes que tumbaban ~1 de cada 3 corridas. Reintenta SOLO EBUSY; EPERM/ENOENT se propagan (un test depende de eso). Nunca `fs` directo.
- **Calca sin precio = null, nunca $0** (#91): las 32 calcas tienen `Menudeo: null` y el fallback a 0 imprimia calcas gratis en PDF y quote. Las piezas de calca NO cuentan para el tier; la calca fija la marca `decorado` y `app.js` la manda solo en `true` (un `false` pisaria la marca del tablero).
- **Las llaves del PUT de cliente NO son las del GET (#169)**: se escribe `cust_name` y se lee `CustName`; se escribe `cfdi_regimen_fiscal` y se lee `regimen`. Mandar la llave de lectura = campo ignorado en silencio (asi el upgrade fiscal dejo un cliente sin razon social). El mapeo por campo vive en `DIFF_FISCAL_CAMPOS` (`write`/`read`); el PUT responde con el **eco** de lo que acepto, y eso es lo que usa `camposNoAplicados` para decirle al vendedor el motivo real.
- **Hay campos que Operam no devuelve en NINGUNA lectura (#373)**: `idcif` e `invoice_email` no vienen ni en `GET /customers/:id` ni en el listado por `?tax_id=` (medido 2026-09-15, cliente 522, Operam 3.26.36: las dos lecturas traen las MISMAS 43 llaves, asi que releer por id no arregla nada). Van marcados `noLegible` en `DIFF_FISCAL_CAMPOS`: NO se comparan -- el panel pintaba `IdCIF (SAT): (vacio) -> 15070019293` sobre un cliente que si lo tenia, y desde #395 el campo sale del bloque `no coinciden` (que afirmaba una diferencia que nadie midio) y se pregunta aparte: `La CSF trae el IdCIF (SAT) X, que puede ser diferente al registrado en Operam. Lo sobrescribimos?` -- pero SI entran al diff y SI se escriben: ese diff ES el cuerpo del PUT del panel (`bodyDesdeDiffFiscal` en el PATCH), asi que sacarlos de ahi seria dejar de escribirlos. Su unica verificacion es el eco del PUT.
- **Un default del formulario no es captura del vendedor (#248/#250)**: el diff fiscal del dedup por RFC se calcula contra `datosFiscalesDelDedup(altaState.datos)`, nunca contra el crudo -- ahi viven el G03 con el que nace el select de uso de CFDI (y le pisaria su S01 al cliente) y el segmento de la Seccion 2, que sigue BLOQUEADA cuando el diff corre; como "Confirmar y actualizar en Operam" es todo-o-nada, la fila legitima arrastraba a las dos. Ese PATCH ademas verifica el eco del PUT con `camposNoAplicados` en vez de responder "actualizados" a ciegas. **#372**: la Seccion 2 son DOS campos, y `sales_type` sale del diff igual que el segmento; el PATCH poda los dos con `diffSinVaciadosComerciales` cuando llegan vacios (Operam los guarda como 0, #285) y los reporta con su motivo REAL -- "no se envio", nunca "Operam lo ignoro", que es lo que dice el eco de un campo que si viajo (#379).
- **El `?search=` de Operam NO indexa el RFC (#194)**: `buscarClientes('XAXX010101000')` devuelve 0 aunque haya 78 clientes con ese RFC — busca por NOMBRE. Toda la dedup que partia de un RFC recibia un pool vacio y concluia "libre" en silencio, con la suite en verde (los mocks contestaban lo que el codigo esperaba). El pool por RFC solo lo da `?tax_id=`: usar `buscarClientesPorRfc(rfc)`, nunca `buscarClientes`. `node scripts/verificar-dedup-rfc.mjs` verifica el supuesto EN VIVO (read-only); ningun test mockeado puede.
- **El `cust_ref` (nombre corto) es UNICO GLOBAL en Operam (#242)**: el POST responde 406 `Already exists customer with same cust_ref`, y la unicidad NO respeta el RFC: el dueno puede ser un cliente con RFC real, invisible para la dedup de genericos. Por eso `poolDedupGenerico` busca el `cust_ref` exacto en el padron COMPLETO cacheado (`clientesCacheados`, `lib/indice-telefonos.js`) ademas de los pools por RFC, y el 406 que igual llegue sale como 409 `CUST_REF_DUPLICADO` con texto accionable. Nunca desambiguar con sufijo automatico: esconde que el cliente ya existia.
- **Quirks de escritura de Operam**: 200/`result:true` no garantiza nada — releer SIEMPRE. `segmento_id` NO se puede escribir por la API v3 por ningun camino y va por post-fix web (#172); `dimension_id` puede ignorarse en silencio; `PUT /branches` huerfana el domicilio sin `customer_id` en el body y responde 406 sin `br_ref` (#386: se relee el `branch_ref` del branch y se reenvia, porque el PUT es REPLACE y omitirlo no lo conserva); no hay DELETE de clientes. Detalle en `docs/arquitectura.md` §Quirks.
- **El celular ligado a otro Cliente Operam es una PREGUNTA, no un 409 (#345, ADR-0016)**: `POST /api/cotizacion/operam/:id` responde **428** `CONFIRMAR_OTRA_RAZON_SOCIAL` con los dos Clientes Operam nombrados y `reintentar` = el cuerpo exacto con el que el navegador reintenta al confirmar (lo dicta el SERVIDOR: candidato elegido, sucursal o camino normal se reintentan distinto). Sin confirmar no se sube nada ni se crea ningun cliente; al confirmar la liga se AGREGA, nunca reemplaza. El 409 que SI queda es el de la cotizacion ya ligada a otro cliente: esa liga es fija.
- **El respaldo al sandbox de Dropbox es por AUSENCIA de configuracion, JAMAS por fallo (#357, padre #354)**: con las dos variables del flujo, un error de Dropbox RECHAZA y ahi termina. Un `catch` que reintente contra la ruta de texto reintroduce el bug: Dropbox CREA la ruta que no existe y responde 200, y asi nacio la replica fantasma del arbol de la empresa dentro del sandbox de la app.
- **El body del quote web lleva `ProcessOrder` y NUNCA `CancelOrder`** (viven en el mismo form; CancelOrder anula la cotizacion).
- **La comparacion de huella del quote (#114) es SIEMPRE local**: el cotizador manda y una edicion hecha directamente en Operam se pierde (decision explicita).
- **Codigos de calca se BUSCAN en el catalogo, nunca se concatenan**: uno inventado da 406 al subir el quote.
- **ASCII estricto** en codigo y commits: sin acentos, sin comillas tipograficas, sin em-dashes.

## Persistencia (reglas vigentes; historia en docs/arquitectura.md y ADR-0008/0009)

- Neon Postgres (`DATABASE_URL`) es la fuente de verdad en produccion; sin `DATABASE_URL` los stores caen a `data/*.json` (dev y tests). El disco de Render es efimero.
- **La tabla `prospectos` es la de CONTACTOS desde #343** (ADR-0016), no la de tarjetas: la Oportunidad pre-cotizacion vive en `oportunidades` y las rutas `/api/prospectos/:id/*` operan sobre ELLA. Los ids de las dos tablas salen del maximo de ambas (espacio compartido). La separacion de lo existente la aplica `node scripts/migrar-oportunidades.mjs --apply` (idempotente; dry-run por defecto), y el codigo funciona igual antes de que corra.
- **La configuracion del panel `/admin` tambien vive en Neon desde #276** (`lib/config-store.js`). `data/config.json` quedo como semilla de la tabla vacia y como fallback de dev: editarlo en un commit ya NO llega a produccion. Expo nueva o liga nueva se configuran DESDE EL PANEL, no con un commit.
- **El numero de la cotizacion ES el folio de Operam** (ADR-0009), nunca el id interno (clave tecnica de URLs). Un solo punto lo decide: `datosDocumento(entry)` en `server.js`. En la UI siempre `etiquetaFolioOperam` (`pipeline-logica.js`).
- Los GET `/api/cotizacion/pdf/:id` y `/html/:id` REGENERAN desde `data` jsonb y son el UNICO camino que genera documento; `POST /api/cotizacion` solo guarda. Van sin auth a proposito (compartir por WhatsApp).
- El flujo guarda → espera subida a Operam → abre el documento numerado; si Operam falla o excede `TIMEOUT_OPERAM_MS`, el documento sale igual como PRE-COTIZACION explicita (nunca sin numero silencioso).
- Regenerar una cotizacion ya subida compara contra `data.huellaQuote` y actualiza el quote conservando folio solo si cambio (#114-#116); con pedido asociado responde 409.
- Gate `puedeActualizarCotizacion`: solo con folio subido, sin pedido asociado y mismo cliente (#104).

## Auth

- Rutas del cotizador: JWT de 30 dias; el registro de vendedores (ID + PIN) vive en `lib/vendedores-store.js` (Neon con fallback al JSON, #141); rol `admin` desbloquea `/api/admin/*`.
- Rutas CSF: mismas garantias (`authMiddleware`). El ciclo de vida del cliente tiene 3 caminos autenticados: alta generica al subir cotizacion, upgrade fiscal (#85, gate anti-fusion por RFC exacto) y alta completa. Detalle en `docs/arquitectura.md` §Auth.
- `server.js` carga `.env` manualmente sin dotenv (lineas ~24-30) y PISA `process.env`.

## Tests

**Backend** (`test/`): ES modules, `node:test` + `supertest`. El app se importa sin `listen()` gracias al guard `isMain` en `server.js`. Helpers de archivos: SIEMPRE via `lib/fs-reintento.js`.

**Frontend** (`public/js/__tests__/`): CommonJS (`.cjs`), sin DOM. `app.js` NO es importable en Node (efectos de navegador en scope de modulo); las funciones puras compartidas viven en modulos intermedios (`alta-logica.js`, importado con `await import()` en un `before()`; el resto en `helpers.cjs` via `require`). No escribir tests tautologicos que afirmen literales que el codigo real nunca construye (leccion de #36).

- **El `.env` local NUNCA debe llevar `DATABASE_URL`**: varias suites escriben via stores y con pool le pegarian a Neon real. La suite de vendedores ademas la borra antes de importar `server.js` (defensa extra); el resto confia en la convencion.
- Mock de Operam: `mockFetchByUrl(urlHandlers)` / `mockOperamFetch(handlers)` interceptan por substring de URL y restauran al terminar.
- Testear PDFs: pasar `_compress: false` y buscar strings con `buffer.toString('latin1').includes(str)`.

## Integraciones externas

- **Operam ERP v3**: `OPERAM_URL` + `OPERAM_USER` + `OPERAM_PASSWORD`. Company ID: `346`. Bearer token.
- **Neon Postgres**: `DATABASE_URL`.
- **Dropbox**: `DROPBOX_REFRESH_TOKEN` + `DROPBOX_APP_KEY` + `DROPBOX_APP_SECRET`. Fire-and-forget. Destino por flujo (#357): `DROPBOX_NS_CSF`/`DROPBOX_PATH_CSF`, `DROPBOX_NS_CALCA`/`DROPBOX_PATH_CALCA`, `DROPBOX_NS_BITRIX`/`DROPBOX_PATH_BITRIX` -- un flujo cuenta como configurado solo con AMBAS.
- **envia.com**: `ENVIA_API_KEY`. FedEx, DHL y UPS en paralelo con `Promise.allSettled`.
- **Shopify**: `SHOPIFY_API_TOKEN` (`lib/shopify-pedidos.js` y `scripts/fetch-shopify-images.js`). Token de app custom del admin que **ya no se puede recrear** (ADR-0014): no se rota sin respaldo. Solo alcanza los ultimos 60 dias de pedidos.
- **SAT**: proxy en `/api/csf-from-url` para QR de CSF sin texto extraible.

## Deploy

Render.com (plan Starter: no duerme, UNA instancia). Auto-deploy desde `main`. Config en `render.yaml`; las env vars viven en el dashboard de Render, no en el yaml.

> Varias piezas asumen **un solo proceso Node**: el lock `subidasOperamEnCurso` y la cola de post-fixes de vigencia viven en memoria. Con varias instancias habria que moverlas a Neon o a un lock distribuido.
